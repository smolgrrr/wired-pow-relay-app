version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: smolgrrr-wired-pow-relay_web_1
      APP_PORT: 3000
      PROXY_AUTH_ADD: "false"

  web:
    image: ghcr.io/smolgrrr/wired-pow-relay-app-web:0.2.1
    restart: on-failure
    stop_grace_period: 30s
    environment:
      PORT: "3000"
      WIRED_DATA_DIR: "/app/data"
      FEED_SNAPSHOT_CACHE_FILE: "/app/data/feed-bootstrap.json"
      WIRED_MODERATION_STORE: "/app/data/moderation.json"
      MODERATION_ADMIN_OPEN: "true"
      PUBLIC_HOSTS: "wiredsignal.online,relay.wiredsignal.online"
      FEED_SNAPSHOT_REFRESH_SECONDS: "300"
      FEED_SNAPSHOT_AGE_HOURS: "24"
      RELAY_BACKEND_URL: "ws://smolgrrr-wired-pow-relay_relay_1:7777"
      RELAY_NAME: "Wired PoW Relay"
      RELAY_DESCRIPTION: "A Wired proof-of-work Nostr relay, feed snapshot service, and moderation console backed by strfry."
      RELAY_CONTACT: ""
      RELAY_PUBKEY: ""
      RELAY_ICON: "https://www.getwired.app/icon.png"
      RELAY_MIN_POW: "16"
      RELAY_SOFTWARE: "https://github.com/smolgrrr/wired-pow-relay-app"
      RELAY_VERSION: "0.2.1"
    volumes:
      - ${APP_DATA_DIR}/data/web:/app/data
    depends_on:
      - relay

  relay:
    image: ghcr.io/hoytech/strfry@sha256:c52807349888ef8a9f8720f3e51ee81af7e20114bad105915febb78f9c604cb1
    user: "1000:1000"
    restart: on-failure
    stop_grace_period: 30s
    working_dir: /etc/strfry
    command: ["relay"]
    volumes:
      - ${APP_DATA_DIR}/data/strfry:/app/strfry-db
      - ${APP_DATA_DIR}/strfry.conf:/etc/strfry/strfry.conf:ro
